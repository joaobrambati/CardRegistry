services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: CardRegistry
    environment:
      SA_PASSWORD: "VaiCorinthians@123"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql

volumes:
  sqlserver_data:
